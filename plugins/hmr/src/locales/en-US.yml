base: 用户显示路径的根目录，默认为当前工作路径。
root: 要监听的文件或目录列表，相对于 `base` 路径。
ignored: Files or directories to ignore. Supports [Glob Patterns](https://github.com/micromatch/micromatch) syntax.
debounce: 延迟触发更新的等待时间。
